/**
 * Re-export of the database contract generated from the local Supabase schema.
 * Refresh it after schema changes with `npm run db:types`.
 */
export type { Database } from "@/types/database.generated";
